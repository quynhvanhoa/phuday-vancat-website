backend:
  name: github
  repo: quynhvanhoa/phuday-vancat-website
  branch: main
  base_url: https://phuday-cms-auth.quynhvanhoa.workers.dev
  auth_endpoint: /authorize

locale: vi
site_url: https://quynhvanhoa.github.io/phuday-vancat-website/

media_folder: assets/images/uploads
public_folder: /phuday-vancat-website/assets/images/uploads

collections:
  - name: posts
    label: Bài viết
    label_singular: Bài viết
    folder: content/posts
    create: true
    slug: "{{year}}{{month}}{{day}}-{{slug}}"
    summary: "{{title}} ({{date}})"
    fields:
      - label: Tiêu đề
        name: title
        widget: string
      - label: Ngày đăng
        name: date
        widget: datetime
        format: "YYYY-MM-DD"
        date_format: "DD/MM/YYYY"
        time_format: false
        default: ""
      - label: Danh mục
        name: category
        widget: select
        default: su-kien
        options:
          - { label: "Sự kiện", value: su-kien }
          - { label: "Lễ hội", value: le-hoi }
          - { label: "Hoạt động xã hội", value: xa-hoi }
          - { label: "Chuyên đề", value: chuyen-de }
      - label: Tóm tắt
        name: excerpt
        widget: text
        required: false
        hint: "Tóm tắt ngắn hiển thị ở trang danh sách (tùy chọn)"
      - label: Ảnh đại diện
        name: thumbnail
        widget: image
        required: false
        hint: "Ảnh hiển thị đầu bài (tùy chọn)"
      - label: Nội dung bài viết
        name: body
        widget: markdown
